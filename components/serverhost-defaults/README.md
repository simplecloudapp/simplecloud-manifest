# internal-workflows component

Place workflow definition files for SimpleCloud under:

- `components/internal-workflows/workflows/internal/`

The release workflow archives this folder and publishes it as:
`internal-workflows.zip`.
